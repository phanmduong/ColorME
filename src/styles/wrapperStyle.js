import {StyleSheet, Dimensions} from 'react-native';

const wrapper = StyleSheet.create({
    // WRAPPER
    wrapperContainer:{
        flex: 1,
    },
    wrapperTop:{
        flex: 1,
    },
    wrapperMid:{
        flex: 11,
    }
});

export default wrapper;